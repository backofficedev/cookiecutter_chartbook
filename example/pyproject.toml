[project]
name = "example"
version = "0.1.0"
description = "Example project"
requires-python = ">=3.10"

[tool.pytest.ini_options]
filterwarnings = ["ignore::Warning"]

[tool.ruff]
line-length = 88

[tool.ruff.lint]
select = ["E", "F", "I"]
